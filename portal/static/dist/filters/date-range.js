export default function initDateRangeFilter(startInput, endInput) {const label = startInput.dataset.label || 'Date';function getValue() {const start = startInput.value;const end = endInput.value;if (!start && !end) return [];const text = `${start || '…'} - ${end || '…'}`;return [{ label, value: text, name: `${startInput.name}|${endInput.name}` }];}return {getValues: getValue,clear() {startInput.value = '';endInput.value = '';},clearValue() {startInput.value = '';endInput.value = '';}};}